{% for type, messages in app.flashes %}
    {% for message in messages %}
        <div class="container alert alert-{{ type }} mt-3" style="padding: 2%;" role="alert">
            <center>{{ message }}</center>
        </div>
    {% endfor %}
{% endfor %}